import { StyleSheet } from "react-native";

const TabStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    padding: 20,
    borderRadius: 4,
    borderWidth: 0.6,
    borderColor: "#d6d7da",
    width: "100%",
    flex: 1
  }
});

export default TabStyles;
